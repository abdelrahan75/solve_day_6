namespace task_day_6
{
    internal class Program
    {
        static void Main(string[] args)
        {
            #region problem_1
            // Point point = new Point(2,3);
            //string ans =  point.ToString();
            // Console.WriteLine(ans);
            // because stack is vlaue type , so allocate in stack
            #endregion

            #region problem_2
            //TypeA ty = new TypeA(5); // to access private 

            //ty.g = 10; // this aceess on same project
            //ty.h = 20; // this public
            //Access modifiers control the scope and visibility of class members such as:
            //fields
            //methods
            //properties
            //constructors

            #endregion

            #region problem_3 
            //Employee employee = new Employee();
            //string ? nam  =  Console.ReadLine();
            //if (nam != null)
            //{
            //    employee.Setname(nam);
            //}
            //string a = employee.Getname();
            //Console.WriteLine(a);
            //Encapsulation is a core principle of Object-Oriented Programming (OOP)
            //that means hiding internal data and allowing access only through controlled methods or properties.

            #endregion

            #region problem_4
            //Point1 p1 = new Point1(2); //first constractor

            //p1.Print();

            //Point1 p2 = new Point1(3,5);
            //p2.Print();

            //A constructor in a struct is a special method used to initialize the struct’s fields when a new struct instance is created.


            #endregion
            #region problme_5
            //Point1 p = new Point1(2, 6);
            //string s =p.ToString();
            //Overriding the ToString() method improves code readability by providing meaningful
            //and human-readable information about an object instead of just showing the class name.
            #endregion



            #region part_2

            //            Copy Constructor هو constructor خاص يُستخدم لإنشاء كائن جديد عن طريق نسخ القيم من كائن آخر من نفس النوع.

            //بمعنى: إنشاء نسخة من كائن موجود بالفعل.


            //An Indexer in C# allows an object to be accessed like an array using square brackets [].
           // It lets you use an index to get or set values inside a class or struct.
            #endregion
            


        }
    }
}
